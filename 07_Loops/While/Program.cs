// while, for, for each, do while

// Declare a variable first (can't be done within loop)

// While Loop
int number = 0;
// while (condition) {body}
while (number <=100) 
{
    Console.WriteLine(number);
    number = number + 5;
}

// If stuck in infinite loop, hit ctrl + c in terminal


